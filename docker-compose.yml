services:
  php:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./www:/var/www/html
    ports:
      - "8080:80"
    depends_on:
      - db
  db:
    build:
      context: .
      dockerfile: Dockerfile-sql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root